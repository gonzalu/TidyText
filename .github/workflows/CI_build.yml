name: CI_build

on: [push, pull_request]

jobs:
  build:

    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        build_platform: [x64]

    steps:
    - name: Checkout repo
      uses: actions/checkout@v7

    - name: Add msbuild to PATH
      uses: microsoft/setup-msbuild@v3

    - name: MSBuild of plugin dll Debug
      working-directory: .\vs.proj
      run: |
           msbuild TidyText.vcxproj /m /p:configuration=Debug /p:platform="${{ matrix.build_platform }}"

    - name: MSBuild of plugin dll Release
      working-directory: .\vs.proj
      run: |
           msbuild TidyText.vcxproj /m /p:configuration=Release /p:platform="${{ matrix.build_platform }}"

    - uses: olegtarasov/get-tag@v2.1.4
      id: tagName

    - name: zip artifacts for Release ${{ matrix.build_platform }}
      run: |
           7z a TidyText_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip .\bin64\TidyText.dll

    - name: Archive artifacts for Release ${{ matrix.build_platform }}
      uses: actions/upload-artifact@v7
      with:
        name: TidyText_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip
        path: TidyText_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip

    - name: Create release on tagging
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: TidyText_${{ steps.tagName.outputs.tag }}_${{ matrix.build_platform }}.zip